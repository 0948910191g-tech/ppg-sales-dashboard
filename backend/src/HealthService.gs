function PPG_HEALTH_get_(deps){try{if(deps&&deps.schemaChecker)deps.schemaChecker();var r=deps.repo,bs=r.read('DB_Import_Batches'),accepted=bs.filter(function(b){return b.Batch_Status==='ACCEPTED';});return {schemaCompatible:true,queueCounts:{queued:bs.filter(function(b){return b.Batch_Status==='QUEUED';}).length},lastAcceptedBatch:accepted.length?accepted[accepted.length-1].Batch_ID:null,dataThrough:null,sourceFreshness:{}};}catch(e){throw new Error('HEALTH_CHECK_FAILED');}}
