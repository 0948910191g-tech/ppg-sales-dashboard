# Domain docs

This project uses a single-context domain documentation layout.

Before exploring or changing domain behaviour, read `CONTEXT.md` at the project root and the relevant decisions under `docs/adr/`. Use the glossary terms defined there in ticket titles, tests, and implementation notes. If a needed concept is missing, flag it for domain modeling instead of inventing a competing term.
